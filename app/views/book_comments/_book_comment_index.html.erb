<% book_show.book_comments.each do |book_comment| %>
  <div>
    <tr>
      <td><%= attachment_image_tag(book_comment.user, :profile_image, size: "40x40", fallback: "no-image-icon.jpg") %></td>
          <td><%= book_comment.comment %></td>
          <% if current_user == book_comment.user %>
      	   <td><%= link_to 'Destroy', book_book_comment_path(book_comment.book.id, book_comment.id), class: 'btn-sm btn-danger',remote: true, method: :delete %></td>
    	    <% end %>
    	 </tr>
    	</div>
      <% end %>

